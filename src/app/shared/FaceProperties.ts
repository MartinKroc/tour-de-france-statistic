export interface FaceProperties {
  faceHeight: number; // winner speed
  faceWidth: number; // total distance
  noseSize: number; // entrants
  noseHeight: number; // finishers
  earCol: string; // winner nation
  eyeCol: string; // winner team
  mouthSmile: number; // race days
  brewLength: number; // number of stages
  year: number;
}
